import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  //improves performance but is experimental and not recommended for production
  // experimental: {
  //   ppr: 'incremental',
  // }
};

export default nextConfig;
